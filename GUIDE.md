# 우리의 맛집 리스트 — 작업 지침서

개인용 맛집 리스트 모바일 웹페이지. `menu` 폴더를 통째로 Netlify에 업로드해서 배포한다.
빌드 도구 없이 순수 HTML/CSS/JS로만 동작한다 (별도 설치 불필요).

## 파일 구조

```
menu/
  index.html   - 페이지 구조 (헤더, 리스트, 사이드바, 라이트박스)
  style.css    - 전체 스타일
  script.js    - 렌더링/필터/라이트박스 로직
  data.js      - 식당 데이터 (내용 편집은 대부분 이 파일만 건드리면 됨)
  images/      - 사진 원본 (png)
```

## 디자인 톤

`../DESIGN-starbucks.md`(스타벅스 디자인 시스템 분석 문서)에서 따온 톤을 사용 중:
- 배경: 따뜻한 크림색 `#f2f0eb`
- 포인트 컬러: 그린 `#00754a` (버튼, 가격, 활성 필터)
- 카드: 흰 배경, `12px` 라운드, 옅은 그림자
- 버튼: `50px` 완전 필(pill) 라운드
- 폰트: Inter (SoDoSans 대체)

## data.js 스키마

```js
{
  name: "정각 망원본점",       // 화면에 표시되는 이름
  imageKey: "정각",           // 사진 파일명 접두어 (name과 달라도 됨)
  region: "망원",             // 지역 필터 값
  category: "돼지고기",       // 음식 종류 필터 값
  mapUrl: "https://naver.me/...",   // 네이버/구글 지도 링크 아무거나 (버튼 텍스트가 자동으로 바뀜)
  photos: ["images/정각_된장술밥.png", "images/정각_정각스페셜.png"], // 추가 사진 (선택)
  menus: [
    {
      name: "정각의 선택",
      price: "59,000원",
      desc: "알등심, 살치살, 목살, 돈치살 정각시그니처 한판", // 선택 항목
      image: "images/정각_메뉴1.png",
    },
    // ... 나머지 메뉴
  ],
}
```

- `menus` 배열의 **첫 번째 항목이 대표 메뉴**로 카드에 바로 노출된다.
- 나머지 메뉴는 "메뉴 더보기" 버튼을 눌러야 펼쳐진다.
- `region`, `category`는 좌측 사이드바 필터 목록에 자동으로 반영된다 (직접 등록할 필요 없음, `data.js`의 값에서 자동 추출).

## 이미지 파일명 규칙

| 용도 | 경로 규칙 | 확대(라이트박스) |
|---|---|---|
| 외관 사진 | `images/{imageKey}_외관.png` | O |
| 내부 사진 | `images/{imageKey}_내부사진.png` | O |
| 메뉴판 사진 | `menus[].image`에 직접 경로 지정 (파일명 자유) | X (제외) |
| 추가 사진 | `photos[]`에 직접 경로 지정 (파일명 자유) | O |

사진이 없거나 파일명이 안 맞으면 자동으로 "사진 없음" 플레이스홀더가 표시된다 (에러로 깨지지 않음).

## 기능 목록

1. **카드 리스트**: 식당별로 외관/내부 사진, 이름, 지역·음식종류 태그, 대표 메뉴, 지도 버튼
2. **메뉴 더보기**: 대표 메뉴 1개 노출 + 나머지는 토글로 펼침/접힘
3. **추가 사진 갤러리**: 메뉴판 사진과 분리된 가로 스크롤 썸네일 목록
4. **분류 필터 사이드바**: 좌측 상단 ☰ 버튼 → 사이드바 슬라이드 → 지역/음식종류 칩 선택 → 리스트 필터링. "필터 초기화"로 전체 보기 복귀
5. **사진 확대(라이트박스)**: 메뉴판 사진을 제외한 모든 사진(외관/내부/추가 사진) 클릭 시 전체화면으로 확대. 모바일 브라우저 기본 핀치줌으로 확대/축소 가능. 배경 클릭, ×버튼, Esc 키로 닫힘

> 비교 기능은 사용하지 않기로 하여 완전히 제거된 상태 (관련 코드 없음).

## 새 식당 추가하는 법

1. `images/`에 사진 저장 (외관/내부/메뉴 사진들/선택적으로 실제 방문 사진)
2. `data.js`의 `restaurants` 배열에 객체 하나 추가 (위 스키마 참고)
3. 그게 끝 — `index.html`, `script.js`, `style.css`는 건드릴 필요 없음

## 로컬 미리보기

이 프로젝트 자체에는 서버가 없으므로 `file://`로 열면 스크립트가 실행되지 않는다.
루트의 `.claude/launch.json`에 `npx serve menu`로 로컬 서버를 띄우는 설정이 등록되어 있음 (Netlify 배포에는 영향 없음, 로컬 확인용).
